// =============================================================================
// Listener role selected at connection acceptance.
//
// The value determines the initial native handshake and Router's role gate; it
// is not a recovered original-server service taxonomy.
// =============================================================================
namespace PaperMan.Server;

public enum ServerRole
{
    Login,
    Channel,
}
